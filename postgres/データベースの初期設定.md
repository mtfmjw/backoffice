# Postgresデータベースの初期設定

以下の手順はPgAdmin4で実行することを想定している。

- デフォルトのpostgresユーザーとしてDBにログインし、下記のSQLを実行してDBユーザーを新規作成する
  - postgres/create_user.sql
- 新規作成したbackofficeユーザーとしてDBにログインし、下記のSQLを実行してデータベースを新規作成する
  - postgres/create_database.sql
