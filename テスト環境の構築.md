# テスト環境の構築

Visual CodeとPgadmin4を利用してローカルテスト用実行環境を構築する

## データベースの構築

1. [Postgresql](https://www.postgresql.org/download/windows/)をインストール
2. [Pgadmin4](https://www.pgadmin.org/download/pgadmin-4-windows/)をインストール
   以後、SQL文はすべてPgadmin4でrole=postgresとして実行すること
3. ロールの作成

   ```sql
   CREATE ROLE backoffice WITH LOGIN ENCRYPTED PASSWORD 'P09olp09ol' CREATEDB;
   ```

4. データベースの作成

   ```sql
   CREATE DATABASE backoffice OWNER backoffice;
   ```

5. 権限の付与

   ```sql
   GRANT ALL PRIVILEGES ON DATABASE backoffice TO backoffice;
    ALTER SCHEMA public OWNER TO backoffice;
    GRANT ALL ON SCHEMA public TO backoffice;
   ```

6. テスト用データベースの復元
   [事前のバックアップ](common/data/)を用いてテスト用データベースを復元する

## ソースの準備

[Github](https://github.com/orgs/LDJP-AI/repositories)よりbackofficeリポジトリをクローンする。
クローン先フォルダーをプロジェクトのroot directoryとする（例えば、backoffice）

```text
backoffice/
├── .venv/ (python仮想環境)
├── backoffice/
│   ├── settings.py
│   ├── urls.py
│   └── ...
├── common/
│   ├── admin/
│   ├── data/
│   ├── locale/
│   ├── migrations/
│   ├── models/
│   ├── apps.py
│   └── ...
├── kintai/
│   ├── admin/
│   ├── locale/
│   ├── migrations/
│   ├── models/
│   ├── static/
│   ├── templates/
│   ├── apps.py
│   └── ...
├── static/
├── templates/
├── ...
├── manage.py
├── requirements.txt
└── テスト環境の構築.md
```

## python環境の構築

1. [python](https://www.python.org/downloads/windows/)のインストール
   以後コマンド操作はプロジェクト・
2. 仮想環境の作成（オプション）

   ```batch
   python -m venv .venv
   ```

3. 日本語メッセージの生成

   ```batch
   python manage.py compilemessages -l ja
   ```

## Django開発用サーバーの起動

```batch
python manage.py runserver
```

## ローカルテスト環境へのログイン

[http://localhost:8000](http://localhost:8000/)

- ログインID：admin
- パスワード：P09olp09ol
