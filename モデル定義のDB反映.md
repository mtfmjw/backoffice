# モデル定義をDBに反映する手順

## 概要

Djangoでは、`models.py` の変更を直接DBに反映することはできません。  
**マイグレーション**というファイルを生成し、それをDBに適用する2段階の手順が必要です。

---

## 手順

### 1. モデルを編集する

`kintai/models.py` を編集してフィールドの追加・変更・削除を行います。

---

### 2. マイグレーションファイルを生成する

```bash
python.exe manage.py makemigrations kintai
```

- `kintai/migrations/` 配下に連番付きのファイル（例: `0006_...py`）が生成されます。
- 変更がない場合は `No changes detected` と表示されます。
- 生成されたファイルはGitで管理します。

---

### 3. マイグレーションをDBに適用する

```bash
python.exe manage.py migrate
```

- 未適用のマイグレーションがすべて実行されます。
- 適用済みのものはスキップされます。

---

## 確認コマンド

| コマンド | 説明 |
|---|---|
| `manage.py showmigrations kintai` | 適用済み／未適用の一覧を表示 |
| `manage.py sqlmigrate kintai 0006` | 実行されるSQLを事前確認 |

---

## マイグレーションファイルの圧縮（squashmigrations）

マイグレーションファイルが増えてきた場合、複数のファイルを1つに統合できます。

```bash
python.exe manage.py squashmigrations kintai 0001 0005
```

- `0001` から `0005` までのマイグレーションを1ファイルに圧縮します。
- 冗長な操作が最適化され、オペレーション数が大幅に削減されます（例: 21→1）。
- 生成されたファイルには `replaces` 属性が含まれ、旧ファイルとの対応が記録されます。

### 圧縮後の手順

1. 生成されたスカッシュファイルをコミットします。
2. すべての環境で `migrate` を実行し、スカッシュが適用済みと認識されたことを確認します。
3. 旧マイグレーションファイル（`0001`〜`0005`）を削除します。

```bash
python.exe manage.py migrate kintai
```

`No migrations to apply.` と表示されれば正常です。

---

## 注意事項

- `makemigrations` だけではDBは変わりません。必ず `migrate` まで実行してください。
- フィールドを削除・リネームする場合、既存データが失われる可能性があります。`sqlmigrate` で事前確認を推奨します。
- チームで開発する場合、マイグレーションファイルは必ずコミットしてください。他の開発者が `migrate` を実行できるようになります。

